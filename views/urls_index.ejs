<!DOCTYPE html>
<html lang="en">

<style>
.page-title{font-family: monospace;}
body{text-align: center;}
.buttons{padding: 10px;border-radius:10px; background-color:#ddd;text-decoration: none;}
.buttons:hover{background-color: blue;color:white;}
nav {
    padding:10px;
    background-color: #ccc;
    width: 50%;
    margin: auto;
}
nav a{
    padding: 20px;
    font-size: 20px;
    text-transform: uppercase;
    text-decoration: none;
    font-family: monospace;
}
.space{padding:30px;}
header form{padding: 30px 0 30px;
             display: inline-block;}
nav a:hover{color:white;}
li{list-style: none;}
</style>
<head>
  <title>TinyApp</title>

</head>
<body>
<header>
  <% include partials/_header %>
  <% include partials/nav %>
</header>
  <h1> HEY YOU'RE DOING IT</h1>

   <ul>
    <% for(var url in ownerOfUrl) { %>
      <li><%= url + ' :  '  %><a href="<%=ownerOfUrl[url]%>" target="_blank"><%= ownerOfUrl[url] %></a>
          <form method="POST" action="/urls/<%= url %>/delete">
               <button type="submit">Delete URL</button>
          </form>
      </li>
    <% } %>

    </ul>
    <div>
    <a class="buttons" href="/urls/new" > new link for shortening url</a>
  </div>
</body>
</html>